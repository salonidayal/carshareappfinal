import { ChangeGreenDirective } from './change-green.directive';

describe('ChangeGreenDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeGreenDirective();
    expect(directive).toBeTruthy();
  });
});
